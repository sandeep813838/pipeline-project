pipeline {
    agent any
    stages {
		stage(‘Build’) {
            steps {
                sh 'git --version'
            }
        }
		stage('test') {
            steps {
                sh 'script.sh'
            }
        }
    }
}

